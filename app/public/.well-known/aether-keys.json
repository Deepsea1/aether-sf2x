{
  "schema": "aether.keys.pointer.v1",
  "live_endpoint": "https://aether.sf2x.com/api/functions/aetherKeys",
  "note": "Static pointer — fetch the live endpoint for current, signed key material."
}
