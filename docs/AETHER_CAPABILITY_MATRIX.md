# Aether Capability Matrix

This matrix is an honest baseline record, not a marketing claim. A capability
becomes `REAL` only after the promotion requirements in the canonical plan are
met in a recorded environment.

| Capability | Baseline state | Evidence / gap |
|---|---|---|
| Integrity-protected warrants and ledger | PARTIAL | deterministic seal and Merkle tests pass locally; staging/live verification is unrecorded |
| APIs and verification functions | PARTIAL | callable code exists; all factual paths do not yet expose the canonical five dimensions |
| MCP worker | PARTIAL | 214 deterministic tests pass; public endpoint handshake is unverified |
| GitHub Action | PARTIAL | 41 deterministic tests pass; authenticated dogfood requires `AETHER_API_KEY` and is unverified |
| JavaScript SDK | BLOCKED | deterministic suite fails under the desktop ESM package boundary |
| Secure retrieval | PARTIAL | worker SSRF controls are tested; canonical hostile-content isolation is incomplete |
| Source identity and origin independence | PARTIAL | origin-graph tests exist; canonical policy enforcement is incomplete |
| Claim compiler / Truth Gate | PARTIAL | extraction and resolver modules exist; no universal canonical gate is evidenced |
| Typed memory and correction propagation | PARTIAL | correction features exist; complete dependency invalidation is unproven |
| Tenant isolation and privacy | PARTIAL | fail-closed unit controls exist; end-to-end isolation evidence is absent |
| Deterministic cognition services | ABSENT | no canonical math, units, timezone, legal-effectivity, or geospatial service set |
| Outcome learning and calibration | PARTIAL | cards and artifacts exist; no holdout-gated strategy loop is evidenced |

No row may be promoted based only on a UI, model response, local fixture, or
unverified report.
