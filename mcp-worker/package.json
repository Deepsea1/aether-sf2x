{
  "name": "aether-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test src/*.test.js"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.10.1"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
